<header class="bg-red-500 text-white p-4 shadow-md">
  <nav class="container mx-auto flex justify-between items-center">
    <ul class="flex space-x-4">
      <li>
        <a
          href="/"
          class="py-2 px-4 rounded transition duration-300 <%= currentPage === 'home' ? 'bg-white text-red-500' : 'bg-red-500' %>"
          >Home</a
        >
      </li>
      <li>
        <a
          href="/host/add-home"
          class="py-2 px-4 rounded transition duration-300 <%= currentPage === 'add-home' ? 'bg-white text-red-500' : 'bg-red-500' %>"
          >Add Home</a
        >
      </li>
    </ul>
  </nav>
</header>
